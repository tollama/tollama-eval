apiVersion: v1
kind: Service
metadata:
  name: {{ include "tollama-eval.fullname" . }}
  labels:
    {{- include "tollama-eval.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tollama-eval.selectorLabels" . | nindent 4 }}
